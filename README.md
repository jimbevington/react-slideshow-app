React app implementing a slideshow, whereby user taps (anywhere onscreen) trigger the next stage in an animated sequence.

Effectively, each stage causes a new element to render or animation to start.
